<ul class="nav justify-content-end context-menu mt-2">
  <li class="nav-item">
    <a class="nav-link dropdown"
       data-toggle="dropdown"
       href="#"
       role="button"
       aria-haspopup="true"
       aria-expanded="false">
      <%= icon(:menu) %>
    </a>
    <div class="dropdown-menu drop-down-menu-right">
      <%= link_to(
        icon(:world) { @fact.world.name },
        world_path(@fact.world),
        class: 'dropdown-item',
        title: t(:world)) %>
      <%= link_to(
        icon(:fact) { @fact.name },
        world_fact_path(@fact.world, @fact),
        class: 'dropdown-item') %>
      <%= link_to(
        icon(:new) { t('new') },
        new_world_fact_relation_path(@fact.world, @fact),
        class: 'dropdown-item',
        title: t(:new)) %>
    </div>
  </li>
</ul>
